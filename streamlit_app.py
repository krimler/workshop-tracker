"""Entry point for Streamlit Community Cloud, which defaults to this filename.

The app lives in app.py; this just runs it so deployment works without having to
set a custom main-file path.
"""
from app import main

main()
